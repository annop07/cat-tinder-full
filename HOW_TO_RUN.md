# 🐱 Cat Tinder - คู่มือการรันโปรเจค

## 📋 ข้อกำหนดเบื้องต้น

- Node.js (v16 ขึ้นไป)
- MongoDB (MongoDB Compass หรือ MongoDB Atlas)
- Expo Go app บนโทรศัพท์ (ดาวน์โหลดจาก App Store/Play Store)
- คอมพิวเตอร์และโทรศัพท์ต้องอยู่ในเครือข่าย WiFi เดียวกัน

---

## 🚀 วิธีการรันโปรเจค

### ขั้นตอนที่ 1: เตรียม MongoDB

**แบบที่ 1: ใช้ MongoDB บนเครื่อง (Local)**
```bash
# ตรวจสอบว่า MongoDB กำลังทำงาน
# เปิด MongoDB Compass แล้วเชื่อมต่อที่ mongodb://localhost:27017
```

**แบบที่ 2: ใช้ MongoDB Atlas (Cloud)**
```bash
# ใช้ connection string จาก MongoDB Atlas
# แก้ไขใน backend_cat-tinder/.env
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/pawmise
```

### ขั้นตอนที่ 2: หา IP Address ของเครื่องคอมพิวเตอร์

เปิด Command Prompt แล้วพิมพ์:
```bash
ipconfig
```

จดบันทึก **IPv4 Address** เช่น `192.168.1.100`

### ขั้นตอนที่ 3: แก้ไข Frontend Configuration

แก้ไขไฟล์ `frontend_cat-tinder/constants/config.ts`:

```typescript
export const API_URL = __DEV__
  ? 'http://YOUR_IP_HERE:5000/api'  // ⬅️ เปลี่ยน YOUR_IP_HERE เป็น IP ที่ได้จากขั้นตอนที่ 2
  : 'https://your-production-api.com/api';
```

**ตัวอย่าง:**
```typescript
export const API_URL = __DEV__
  ? 'http://192.168.1.100:5000/api'  // ✅ ใช้ IP จริง
  : 'https://your-production-api.com/api';
```

### ขั้นตอนที่ 4: รัน Backend

เปิด Terminal แล้วรันคำสั่ง:

```bash
cd backend_cat-tinder
npm install        # ครั้งแรกเท่านั้น
npm run dev       # รัน backend server
```

**ผลลัพธ์ที่คาดหวัง:**
```
🚀 Server is running on port 5000
🔌 Socket.IO server is ready
✅ MongoDB connected successfully
```

### ขั้นตอนที่ 5: รัน Frontend (Expo)

เปิด Terminal ใหม่แล้วรันคำสั่ง:

```bash
cd frontend_cat-tinder
npm install                    # ครั้งแรกเท่านั้น
npx expo start --clear        # รัน Expo dev server
```

### ขั้นตอนที่ 6: เปิดแอพบนโทรศัพท์

1. เปิดแอพ **Expo Go** บนโทรศัพท์
2. สแกน QR code ที่แสดงใน Terminal
3. รอให้แอพโหลด

---

## 🔄 เมื่อย้ายไปรันที่เครื่องอื่น / WiFi อื่น

### สิ่งที่ต้องทำ:

1. **หา IP Address ใหม่**
   ```bash
   ipconfig
   ```

2. **แก้ไข `frontend_cat-tinder/constants/config.ts`**
   ```typescript
   export const API_URL = __DEV__
     ? 'http://NEW_IP_HERE:5000/api'  // ⬅️ เปลี่ยนเป็น IP ใหม่
     : 'https://your-production-api.com/api';
   ```

3. **Restart Expo server**
   ```bash
   # กด Ctrl+C เพื่อหยุด
   npx expo start --clear
   ```

4. **สแกน QR code ใหม่**

### ⚠️ ข้อควรระวัง:

- **WiFi ต้องเหมือนกัน**: คอมพิวเตอร์และโทรศัพท์ต้องอยู่ในเครือข่าย WiFi เดียวกัน
- **Firewall**: ถ้าเชื่อมต่อไม่ได้ อาจต้องปิด Windows Firewall ชั่วคราว หรือเพิ่ม Node.js เข้า exception
- **MongoDB**: ถ้าใช้ MongoDB Atlas ไม่ต้องกังวล แต่ถ้าใช้ local MongoDB ต้องติดตั้งบนเครื่องที่รัน

---

## 🐛 แก้ไขปัญหาทั่วไป

### ❌ Network request failed

**สาเหตุ:** โทรศัพท์เชื่อมต่อ backend ไม่ได้

**แก้ไข:**
1. ตรวจสอบว่าใช้ WiFi เดียวกัน
2. ตรวจสอบว่า IP Address ใน config ถูกต้อง
3. ทดสอบเปิดเบราว์เซอร์บนโทรศัพท์ แล้วเข้า `http://YOUR_IP:5000`
4. ปิด Windows Firewall ชั่วคราว

### ❌ MongoDB connection error

**สาเหตุ:** MongoDB ไม่ทำงาน หรือ connection string ผิด

**แก้ไข:**
- ถ้าใช้ local: เปิด MongoDB Compass
- ถ้าใช้ Atlas: ตรวจสอบ username/password และ IP whitelist

### ❌ Token expired

**สาเหตุ:** Token หมดอายุหรือ user ถูกลบ

**แก้ไข:**
- Uninstall แอพแล้วติดตั้งใหม่
- หรือลบข้อมูล Expo Go app

---

## 📱 ทางเลือกอื่นๆ สำหรับการทดสอบ

### 1. ใช้ Expo Tunnel (ไม่ต้องอยู่ WiFi เดียวกัน)

```bash
npx expo start --tunnel
```

**ข้อดี:** ใช้งานได้ทุกเครือข่าย
**ข้อเสีย:** ช้ากว่า, ต้องติดตั้ง `@expo/ngrok`

### 2. ใช้ Android Emulator หรือ iOS Simulator

**Android Emulator:**
```typescript
// ใน config.ts
export const API_URL = __DEV__
  ? 'http://10.0.2.2:5000/api'  // Special IP for Android Emulator
  : 'https://your-production-api.com/api';
```

**iOS Simulator:**
```typescript
// ใน config.ts
export const API_URL = __DEV__
  ? 'http://localhost:5000/api'  // localhost works on iOS Simulator
  : 'https://your-production-api.com/api';
```

---

## 📝 สรุปคำสั่งสำคัญ

```bash
# หา IP Address
ipconfig

# รัน Backend
cd backend_cat-tinder
npm run dev

# รัน Frontend
cd frontend_cat-tinder
npx expo start --clear

# ล้าง cache (ถ้ามีปัญหา)
cd frontend_cat-tinder
npx expo start --clear
```

---

## 💡 Tips สำหรับการพัฒนา

1. **ใช้ MongoDB Atlas** - ไม่ต้องกังวลเรื่อง local database
2. **ใช้ iOS Simulator** - สะดวก ไม่ต้องหา IP Address
3. **เก็บ IP Address** - จด IP ของ WiFi บ้าน/ออฟฟิศไว้
4. **Commit บ่อยๆ** - ใช้ Git เก็บ version

---

**หากมีปัญหาเพิ่มเติม สามารถเช็คได้ที่:**
- Backend logs ใน Terminal ที่รัน `npm run dev`
- Frontend logs ใน Expo Dev Tools หรือ Metro bundler
- MongoDB Compass สำหรับดูข้อมูลใน database
